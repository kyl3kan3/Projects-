/**
 * src/app/page.tsx
 *
 * Marketing landing page. Built LAST, to MARKETING_PLAYBOOK.md.
 *
 * Message architecture:
 * - Enemy: a volunteer treasurer chasing 40 checks a quarter.
 * - One sentence: "Dues collect themselves, records keep themselves, and
 *   the next treasurer inherits a working system."
 * - Device: 40 checks -> 3 -- the checks-to-chase counter rolling down as
 *   autopay enrollments land.
 *
 * TODO:
 * - [ ] Hero: the machine running -- invoices going out, PAID seals
 *       stamping in as autopay settles, the counter rolling down
 *       (beat 1 of 4).
 * - [ ] The device section: "40 checks -> 3" with the enrollment curve
 *       (beat 2).
 * - [ ] The math: treasurer evenings per quarter x their hourly worth vs
 *       $49; anchored also against $10-25/door professional management.
 * - [ ] Receipts (Law 5): a real dues cycle from a permissioned design-
 *       partner association (or our own dogfood club), clearly framed --
 *       never fabricated.
 * - [ ] Objection killer: "Our members won't use software" -> no accounts,
 *       no passwords; grandma pays from a link. Show the portal.
 * - [ ] Pricing anchored per door per month; one CTA phrase repeated
 *       verbatim at hero / post-proof / post-pricing / sticky mobile bar.
 * - [ ] Four animated beats total; everything else static; sub-2s LCP.
 */

export default function LandingPage() {
  return null; // TODO: implement
}

/**
 * Drizzle schema (see ARCHITECTURE.md): users, lists, signups, rewards,
 * reward_grants, blasts, events, webhook_endpoints, webhook_deliveries.
 * TODO: define tables; unique (list_id, email); referral_code index;
 * incremental position update strategy.
 */
export {};

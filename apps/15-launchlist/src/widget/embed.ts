/**
 * Embeddable signup widget (vanilla TS, <10KB gzipped, Shadow DOM).
 * TODO: form render from data-list-id, signup POST, position + share-link
 * success state, theming via CSS custom properties.
 */
export {};

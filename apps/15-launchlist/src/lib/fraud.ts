/**
 * Fraud heuristics: disposable-domain blocklist, plus-alias collapsing,
 * IP-hash velocity clustering, review-queue scoring.
 * TODO: implement scoring 0-100; quarantine >= threshold.
 */
export {};

/**
 * src/app/page.tsx
 *
 * Marketing landing page. Built LAST, to MARKETING_PLAYBOOK.md.
 *
 * Message architecture:
 * - Enemy: doing the work, then waiting 74 days to get paid — and the
 *   awkwardness of asking for money you've already earned.
 * - One sentence: "PaidWell asks for your money so you don't have to —
 *   politely, relentlessly, in your voice."
 * - Device: the DSO number rolling down (47d -> 31d) beside dollars landing.
 * - Hero: a follow-up email composing itself in the firm's voice, then the
 *   settle rule drawing under a paid invoice (the 5-second demo).
 *
 * TODO:
 * - [ ] Hero with the sequence-to-settle demo (HTML/CSS, LCP <= 2s).
 * - [ ] The math section: your aging report vs. 30-days-flat, calculated live.
 * - [ ] Honest receipts (design-partner numbers only, clearly framed).
 * - [ ] Objection killer: "Will it annoy my clients?" -> tone presets +
 *       approval mode, shown with real ladder copy.
 * - [ ] Pricing table anchored against one written-off invoice.
 * - [ ] One CTA phrase repeated verbatim: "Run your aging audit".
 */

export default function LandingPage() {
  // TODO: implement per MARKETING_PLAYBOOK.md
  return null;
}

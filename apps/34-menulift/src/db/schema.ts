/**
 * src/db/schema.ts — Drizzle schema
 *
 * TODO:
 * - [ ] restaurants, locations, members (roles: owner/manager/staff-pin)
 * - [ ] menus, sections, items (price cents, plate_cost cents, tags, photo keys)
 * - [ ] eighty_sixes (item_id, at, by, note, restored_at)
 * - [ ] sales_imports + sales_rows (POS CSV), item_matches
 * - [ ] price_windows (happy hour scheduling)
 * - [ ] subscriptions (Stripe, per location)
 */
export {};

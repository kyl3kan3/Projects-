/**
 * src/lib/qr.ts — QR + print sheet generation
 *
 * TODO:
 * - [ ] per-location and per-table QR (SVG); short slugs
 * - [ ] table-tent PDF sheet at 300dpi with crop marks
 * - [ ] scan counts (privacy-safe, no PII)
 */
export {};

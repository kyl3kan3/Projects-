/**
 * src/app/menu-public.tsx (move to src/app/m/[slug]/page.tsx when built)
 *
 * The page guests hit from the table QR. Must be fast on hotel wifi.
 *
 * TODO:
 * - [ ] server-rendered menu with sections, prices, dietary tags
 * - [ ] 86'd items: hidden or 'back at 9pm' per restaurant setting
 * - [ ] price windows applied by local time
 * - [ ] AVIF/WebP photos, lazy, LCP < 1.5s target; no client JS beyond
 *       allergen filter; Lighthouse mobile >= 90 (ROADMAP gate)
 */
export {};

/**
 * src/app/api/import.ts (move to src/app/api/imports/route.ts when built)
 *
 * TODO:
 * - [ ] upload -> R2, enqueue parse job (worker)
 * - [ ] column-mapping wizard payloads (Toast/Square/Clover presets)
 * - [ ] item-match review: fuzzy candidates, manual override, remember
 */
export {};

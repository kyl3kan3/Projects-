/**
 * src/app/eightysix.tsx (move to src/app/(app)/86/page.tsx when built)
 *
 * Staff-PIN screen; the two-tap kill switch.
 *
 * TODO:
 * - [ ] PIN gate (per location) without full account login
 * - [ ] every item, one tap 86 / restore; optional 'back at' note
 * - [ ] revalidate public menu within seconds
 * - [ ] nightly auto-restore option; action log (who, when)
 */
export {};

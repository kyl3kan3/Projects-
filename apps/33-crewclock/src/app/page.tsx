/**
 * src/app/page.tsx
 *
 * Marketing landing page, per MARKETING_PLAYBOOK.md. Enemy: paper
 * timesheets rounded up, and jobs that lose money silently. Device: the
 * gap between hours paid and hours on site — "the timesheet that can't
 * be rounded up" — proven by labor cost vs bid, live.
 *
 * TODO:
 * - [ ] Hero: the machine running — a staged clock-in draws the geofence
 *       ring, then the live job-cost meter starts ticking (the one
 *       signature animation, reused once here).
 * - [ ] Math section: 15 min/day rounding x crew size x $28 loaded rate,
 *       quantified in front of the visitor (a small crew-size slider).
 * - [ ] Bilingual shown honestly: the same crew screen in EN and ES side
 *       by side — the feature is the ad; ship a full ES page, not a badge.
 * - [ ] Receipts: real design-partner numbers only (Law 5 — never
 *       fabricated); until then, our own dogfood punches, clearly framed.
 * - [ ] Objection killer: "my crew won't use an app" — the 10-second,
 *       gloves-on clock-in as a sequence.
 * - [ ] Pricing anchored against the leak; one CTA phrase repeated at
 *       hero / post-proof / post-pricing / StickyCTA mobile bar.
 * - [ ] Four-beat motion budget; reduced-motion path collapses to fades.
 */

export default function MarketingPage() {
  return null; // TODO: implement
}

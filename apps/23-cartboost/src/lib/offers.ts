/**
 * Offer engine: funnel trigger-rule evaluation by priority, A/B assignment
 * (deterministic hash), downsell step logic, free-tier revenue-cap check.
 * TODO: implement; must respond <200ms (blocks the thank-you transition).
 */
export {};

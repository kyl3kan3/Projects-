/**
 * Workers: webhook event processing + revenue attribution, daily rollups,
 * nightly order-pair mining for AI suggestions (support/confidence).
 * TODO: implement; attribution must reconcile exactly with Shopify orders.
 */
export {};

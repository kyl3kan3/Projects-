/**
 * Drizzle schema (see ARCHITECTURE.md): shops, funnels, offers, ab_tests,
 * offer_events, revenue_rollups, suggestions.
 * TODO: define tables; encrypted access_token column; rollup uniqueness
 * (shop_id, day); event idempotency on webhook retries.
 */
export {};

/**
 * src/app/page.tsx
 *
 * Marketing landing page. Built LAST, to MARKETING_PLAYBOOK.md.
 *
 * Message architecture:
 * - Enemy: the January shoebox and the $400 cleanup fee.
 * - One sentence: "Forward it or photo it -- your accountant gets a clean
 *   close package every month."
 * - Device: shoebox -> ruled ledger (a receipt photo becoming a confirmed,
 *   ruled-off entry, ending on the close package).
 *
 * TODO:
 * - [ ] Hero: the machine running -- a receipt photo extracting into fields
 *       with confidence flags, then the settle rule drawing (beat 1 of 4).
 * - [ ] The device section: "12 months of shoebox = one $400 cleanup fee.
 *       12 months of LedgerLens = $228." (beat 2).
 * - [ ] The math: cleanup-fee calculator inline (hours x accountant rate).
 * - [ ] Receipts (Law 5): a real close package from our own dogfood books,
 *       clearly framed -- never fabricated testimonials.
 * - [ ] Objection killer: "What if the AI reads it wrong?" -> confidence
 *       flags + review queue, nothing unreviewed reaches your export.
 * - [ ] Pricing anchored against the cleanup fee; one CTA phrase repeated
 *       verbatim at hero / post-proof / post-pricing / sticky mobile bar.
 * - [ ] Four animated beats total; everything else static; sub-2s LCP.
 */

export default function LandingPage() {
  throw new Error("Not implemented");
}

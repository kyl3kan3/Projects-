// The single SVG icon set — 20×20 viewBox, stroke 1.75, round caps/joins,
// currentColor, via react-native-svg. No emoji anywhere, no mixed sets.
// Glyphs (DESIGN.md): checklist, folder-vault, camera, file-text, scale,
// seal (circled asterisk — the record mark), hash, house, bank, pie,
// calculator, lock, bell, check, x, plus, chevron-left, chevron-right,
// chevron-down, settings, share, calendar, shield, link.
//
// TODO:
// - [ ] Icon component: name prop → path set; size (tab 22 / inline 18) and color via currentColor
// - [ ] Author each glyph's paths on the 20×20 grid at 1.75 stroke
// - [ ] Accessibility labels for interactive uses
export {};

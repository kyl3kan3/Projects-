// ChecklistRow — the ledger-line task row: full-bleed, hairline rule between,
// min-height 56, 24px checkbox (radius 6; done = verdigris fill + paper check),
// task Title, state-note Secondary, right chevron. Done rows dim to ink-3 —
// never strikethrough (a ledger never crosses out).
//
// TODO:
// - [ ] Row + checkbox states (todo/in_progress/done/na) with 120ms feedback + haptic
// - [ ] State-note line rendered only when the user's state has a branch note
// - [ ] Long-press → status action sheet (mark n/a, add note)
// - [ ] VoiceOver: checkbox state + task title announced together
export {};

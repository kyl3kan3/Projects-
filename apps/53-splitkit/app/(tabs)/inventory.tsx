// Inventory — assets & accounts plus the document vault. Totals strip first
// (Joint / Hers / Unknown, signed sums with debts negative, oxblood total
// rule), asset rows grouped by kind, then the vault section: document cards
// (2-col grid) with hash lines, capture button in the thumb zone.
//
// TODO:
// - [ ] Totals strip computed in repositories (signed cents; arithmetic only, no characterization)
// - [ ] Asset rows per DESIGN.md (kind glyph, JBM values, titling chip); add/edit form sheet (ruled fields)
// - [ ] Asset detail sheet: linked vault documents with hash lines + "Add document"
// - [ ] Vault grid: document cards with thumbnail, kind, SHA-256 hash line
// - [ ] Capture button → /capture; free-tier gates (10 assets, 5 documents)
// - [ ] Empty states: "No accounts yet. Start with where the paychecks land."
export {};

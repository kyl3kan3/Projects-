/**
 * Build-time SQLite access (better-sqlite3): listings, categories, locations,
 * sponsorships, clicks aggregates.
 * TODO: schema bootstrap + typed query helpers used by page generators.
 */
export {};

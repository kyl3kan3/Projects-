/**
 * Programmatic page generators: getStaticPaths sources for category,
 * category x location, best-for, A-vs-B, alternatives pages.
 * TODO: implement matrix builders + THIN-CONTENT GUARD (exclude pages whose
 * unique-data score < threshold from build AND sitemap).
 */
export {};

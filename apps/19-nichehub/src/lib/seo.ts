/**
 * SEO plumbing: schema.org JSON-LD builders (LocalBusiness /
 * SoftwareApplication / ItemList / FAQPage), canonical rules, OG images.
 * TODO: implement per page type; validate against Rich Results test.
 */
export {};

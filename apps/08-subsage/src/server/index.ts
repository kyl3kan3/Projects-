/**
 * Companion API (Node): Gmail OAuth + receipt classification.
 * TODO: /auth/google, /scan (search receipt senders, classify w/ Claude,
 * return normalized {merchant, amount, period} — never store email bodies),
 * /push (renewal reminders via Expo push).
 */
export {};

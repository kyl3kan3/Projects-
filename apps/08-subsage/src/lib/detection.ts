/**
 * Subscription detection from Gmail receipts (via companion server).
 * TODO: OAuth connect flow; server returns detected subs -> user confirms;
 * price-hike detection = new receipt amount > last known.
 */
export {};

/**
 * src/app/page.tsx
 *
 * Marketing landing page. Built LAST, to MARKETING_PLAYBOOK.md.
 *
 * Message architecture:
 * - Enemy: the index-card ledger — the forgotten stripe, the missed
 *   grading, the student who quietly quit three weeks before anyone
 *   noticed.
 * - One sentence: "Every stripe earned, on the wall and on record."
 * - Device: the belt bar. Check-ins tick the counter, the progress bar
 *   fills toward eligibility, a stripe slides on and seats with a snap —
 *   then the grading list adds the student's name by itself.
 * - Hero: the belt bar filling + stripe seating (HTML/CSS, LCP <= 2s),
 *   then the grading event assembling beneath it.
 *
 * TODO:
 * - [ ] Hero with the device; de-risk line: "No card required. Import
 *       your students in an evening."
 * - [ ] Proof: a permissioned design-partner grading event with near-miss
 *       deltas; the retention flag that saved a student ("flagged day 12,
 *       family called, student stayed"); a promotion-history timeline.
 * - [ ] Objection killer: "My whiteboard works fine." -> the quiet quit
 *       the whiteboard can't see; show the drop-off alarm.
 * - [ ] The math: one saved $150/mo family vs $99/mo software, plus the
 *       hours before every grading, refunded.
 * - [ ] Pricing table anchored vs Kicksite/Zen Planner/Gymdesk and vs the
 *       whiteboard.
 * - [ ] One CTA phrase repeated verbatim (hero, post-proof, post-pricing,
 *       sticky mobile bar): "Start free — 14 days".
 */

export default function LandingPage() {
  // TODO: implement per MARKETING_PLAYBOOK.md
  return null;
}

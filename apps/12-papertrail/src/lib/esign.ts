/**
 * E-signature flow with ESIGN-compliant audit trail.
 * TODO: consent capture; typed + drawn signatures; IP/UA/timestamp record;
 * countersign; post-signature immutability (void + reissue only).
 */
export {};

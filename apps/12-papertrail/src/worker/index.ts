/**
 * Worker: reminder sequences (BullMQ delayed jobs) + Puppeteer PDF rendering
 * of signed documents to S3.
 * TODO: implement; reminders stop on payment; PDF render reuses the public
 * doc view for zero drift.
 */
export {};

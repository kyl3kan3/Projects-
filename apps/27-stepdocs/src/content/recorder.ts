/**
 * Recorder content script: interaction capture (click/type/navigate),
 * element-context extraction (accessible name, selector, URL), typed-input
 * summarization (never log keystrokes verbatim in password/sensitive fields).
 * TODO: implement event listeners + step assembly; SPA route-change hooks.
 */
export {};

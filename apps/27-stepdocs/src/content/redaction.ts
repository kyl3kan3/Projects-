/**
 * Local redaction assist: tesseract.js OCR over captured screenshots +
 * PII regexes (emails, phones, card-like numbers, names-in-context) ->
 * suggested blur regions BEFORE upload.
 * TODO: implement; conservative defaults; user confirms suggestions.
 */
export {};

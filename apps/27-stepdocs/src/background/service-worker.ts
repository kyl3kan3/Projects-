/**
 * Service worker: recording session state, captureVisibleTab screenshots,
 * crop coordination, batched upload to the API, auth token storage.
 * TODO: implement message bus (START/STOP/STEP_CAPTURED/UPLOAD).
 */
export {};

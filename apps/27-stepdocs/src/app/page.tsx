/**
 * Marketing landing page. Built LAST, to MARKETING_PLAYBOOK.md.
 *
 * Message architecture:
 * - Enemy: the SOP that was never written, because writing it costs an hour
 *   and the person who knows the task is the busiest person on the team.
 * - One sentence: "Click record, do the task once, get the guide."
 * - Device: a recorded workflow assembling itself into annotated steps.
 * - CTA phrase, verbatim everywhere: "Record your first guide — free".
 *
 * TODO:
 * - [ ] Hero: the extension recording, steps landing one by one, redaction
 *       blurring a token in place (beat 1).
 * - [ ] The device section: a finished guide scrolling — numbered steps,
 *       annotated screenshots, editable copy (beat 2).
 * - [ ] The math: one hour of a senior engineer's time vs $12/seat/mo (beat 3).
 * - [ ] Receipts (Law 5): guides we generated for our own onboarding, clearly
 *       framed as ours -- never fabricated customer logos.
 * - [ ] Objection killer: "our screens have customer data" -> redaction runs
 *       on-device before anything uploads.
 * - [ ] Pricing anchored; CTA phrase repeated verbatim at hero / post-proof /
 *       post-pricing / sticky mobile bar (beat 4: the offer).
 * - [ ] Four animated beats total; everything else static; sub-2s LCP.
 */
export default function LandingPage() {
  return null; // TODO: implement
}

/**
 * MV3 service worker: session token storage, message bus between content
 * script and API, quota cache, install/update lifecycle.
 * TODO: implement message handlers (DRAFT_REQUEST, AUTH_STATUS, USAGE).
 */
export {};

/**
 * Gmail content script: UI injection + thread extraction.
 * TODO: InboxSDK compose-toolbar button ("Draft reply"); thread text extraction
 * from DOM; streamed draft insertion into compose; tone-control chip row;
 * defensive fallback selectors + failure telemetry ping.
 */
export {};

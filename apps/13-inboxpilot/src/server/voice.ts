/**
 * Voice training: consented gmail.readonly fetch of sent mail -> derived
 * style features (greeting, sign-off, sentence stats, phrases) -> store
 * profile ONLY, discard bodies immediately.
 * TODO: implement extraction + profile schema.
 */
export {};

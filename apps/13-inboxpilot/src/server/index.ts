/**
 * Fastify API: /auth/google, /draft (streams Claude response), /usage,
 * /billing/checkout, /billing/webhook, /voice/train (Pro).
 * TODO: implement; quota enforcement server-side; style-profile injection.
 */
export {};

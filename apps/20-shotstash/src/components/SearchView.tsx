/**
 * Search-as-you-type library view with in-image match highlighting.
 * TODO: virtualized grid, debounced FTS queries, bbox overlay renderer,
 * filters (tag/app/date), keyboard-first navigation.
 */
export default function SearchView() {
  return null; // TODO: implement
}

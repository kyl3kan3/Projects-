/**
 * Frontend -> Tauri IPC wrappers (typed invoke calls): search, capture,
 * import, tags, collections, license.
 * TODO: implement with @tauri-apps/api invoke + zod-validated payloads.
 */
export {};

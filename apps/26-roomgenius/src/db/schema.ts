/**
 * Drizzle schema (see ARCHITECTURE.md): users, rooms, renders, styles,
 * products (pgvector embedding), hotspots, credit_ledger, staging_batches.
 * TODO: define tables; credit ledger is append-only; vector index on
 * products.embedding.
 */
export {};

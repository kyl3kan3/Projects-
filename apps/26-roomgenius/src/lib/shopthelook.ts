/**
 * Shop-the-look: furniture detection (bboxes) -> crop embeddings ->
 * pgvector similarity over in-stock affiliate products -> hotspot records
 * -> tracked affiliate outclicks.
 * TODO: implement; category + price-band filters; feed-layer abstraction.
 */
export {};

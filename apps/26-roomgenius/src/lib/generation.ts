/**
 * Generation adapter over Replicate: depth/edge conditioning extraction,
 * style prompt packs, QA scoring (structural-similarity heuristics for
 * melted-geometry detection), single auto-retry.
 * TODO: implement; credits decrement only on QA-passed renders.
 */
export {};

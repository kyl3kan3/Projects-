/**
 * src/app/(app)/talks/page.tsx
 *
 * Dashboard home: this week's compliance at a glance. Mobile-first at
 * 390px per DESIGN.md ("Dashboard home").
 *
 * TODO:
 * - [ ] Server component: this week's talk_instances per crew + the
 *       compliance stat ("11 OF 12 CREWS SIGNED", mono).
 * - [ ] Crew rows: Title crew name, mono talk/status line, orange dot on
 *       missed, chevron to instance detail (attendance + signatures).
 * - [ ] Attendance matrix (employees x weeks) in its own overflow track.
 * - [ ] Talk library drawer: seeded talks by hazard tag, custom upload,
 *       printable render link.
 * - [ ] Thumb-zone primary: **Send this week's talk** (fan-out for crews
 *       not yet delivered).
 * - [ ] Empty state: first-run cards (add a crew, add employees, send the
 *       first talk).
 */

export default function TalksPage() {
  return null; // TODO: implement
}

/**
 * Gigs — the pipeline (DESIGN.md screen 1).
 *
 * Gig cards by status with marquee strips; calendar toggle; conflict
 * warnings inline in redlight naming the other gig.
 *
 * TODO: pipeline sections from the cache-then-server read; the
 * calendar month grid with miniature strips; new-gig sheet.
 */

import { Text, View } from "react-native";

export default function GigsScreen() {
  return (
    <View>
      <Text>Not implemented: pipeline, calendar, conflicts.</Text>
    </View>
  );
}

/**
 * Setlists — the book + the builder (DESIGN.md screen 3).
 *
 * Songs with key/BPM/duration in mono; drag into sets
 * (react-native-draggable-flatlist); total set time computing live;
 * share action mints the music-stand link.
 *
 * TODO: song CRUD; the builder with live totals; duplicate-per-gig.
 */

import { Text, View } from "react-native";

export default function SetlistsScreen() {
  return (
    <View>
      <Text>Not implemented: the book, the builder, share links.</Text>
    </View>
  );
}

/**
 * Paywall — free vs Band (DESIGN.md screen 7).
 *
 * The honest sentence ("Solo is free forever — Band adds the
 * business layer"), what each includes, $12/mo via RevenueCat,
 * purchase + restore, always-visible close.
 *
 * TODO: Purchases.getOfferings/purchase/restore; entitlement refresh.
 */

import { Text, View } from "react-native";

export default function PaywallScreen() {
  return (
    <View>
      <Text>Not implemented: GigBag Band paywall.</Text>
    </View>
  );
}

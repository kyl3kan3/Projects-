/**
 * src/app/(dashboard)/participants/page.tsx
 *
 * The check-in dashboard + participant database -- WaiverWing's staff
 * home (DESIGN.md "Check-in (staff home)"). Auth.js-protected.
 *
 * TODO:
 * - [ ] Header: location switcher + mono day stat (`214 SIGNED ·
 *       186 IN`).
 * - [ ] Hero search field: search-as-you-type via lib/search; results
 *       replace today's board with a 120ms crossfade.
 * - [ ] Today board: signed rows newest-first, pine dots for checked-in,
 *       coverage pills; row select -> thumb-zone primary **Check in**
 *       (or **Send re-sign link** on amber/ember).
 * - [ ] Participant detail: coverage pill, guardian/minor link rows,
 *       signature history with mono evidence lines, **Export PDF**
 *       primary -- the three-tap lawsuit demo.
 * - [ ] The blaze pine-dot pop on check-in; reduced-motion fallback.
 * - [ ] Empty state (new location): QR poster download CTA with real
 *       copy; loading/error states per DESIGN.md.
 */

export default function ParticipantsPage() {
  return null; // TODO: implement
}

/**
 * Drizzle schema for LumaShot.
 * Entities: users, orders (credit packs), training_jobs (LoRA per user),
 * generation_jobs, photos (uploads + results), styles.
 * TODO: define tables; order -> entitled shot count; job status lifecycle.
 */
export {};

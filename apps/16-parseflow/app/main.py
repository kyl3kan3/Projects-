"""FastAPI entrypoint for ParseFlow.

TODO:
- [ ] mount /v1 routers (parse, keys, usage)
- [ ] API-key auth middleware + per-key rate limits
- [ ] hosted playground at / (upload a doc, see JSON)
- [ ] OpenAPI customization — DX is the product
"""

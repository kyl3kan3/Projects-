/**
 * src/app/(dashboard)/apis/[slug]/page.tsx
 *
 * The diff view — the money screen: verdict stamp, finding cards with
 * mini-diffs and the strike-draw signature, consumer impact, timeline
 * compare mode (DESIGN.md).
 *
 * TODO:
 * - [ ] Verdict stamp (JBM display, break/amber/green) + mono counts.
 * - [ ] Finding cards ordered breaking -> risky -> compatible (collapsed);
 *       mini-diff excerpts in their own overflow-x tracks.
 * - [ ] The strike-draw on the top three breaking findings, once per view;
 *       reduced-motion renders complete.
 * - [ ] Ack flow: hold-to-confirm + required note.
 * - [ ] Deploy timeline rail with two-row compare arming.
 * - [ ] First-run empty state: the copyable `npx schemasentry push ...`
 *       terminal block, no illustration.
 */

export default function ApiDiffPage() {
  return null; // TODO: implement
}

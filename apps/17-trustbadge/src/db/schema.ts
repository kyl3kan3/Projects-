/**
 * Drizzle schema: stores, orders, review_requests, reviews (text/photo/video),
 * widgets (type, theme, placement), imports, subscriptions.
 * TODO: define tables; review moderation states; per-store monthly order count
 * (drives plan limits).
 */
export {};

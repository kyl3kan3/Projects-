/**
 * Embeddable widget (vanilla TS, zero deps) — THE product surface.
 * Perf budget: <15KB gzipped, <30ms to first paint, no layout shift.
 *
 * TODO:
 * - [ ] loader: read data-store-id, fetch cached JSON from CDN edge
 * - [ ] renderers: star badge, carousel, wall, floating proof toasts
 * - [ ] Shadow DOM isolation; themable via CSS custom properties
 * - [ ] schema.org Review/AggregateRating JSON-LD injection (SEO for the store)
 */
export {};

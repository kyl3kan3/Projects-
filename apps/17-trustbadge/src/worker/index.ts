/**
 * Worker: post-purchase review-request sequences + review imports.
 * TODO: delay rules per store; email (Resend) + SMS (Twilio) sends w/ deep-link
 * review form; photo-incentive coupon issuance; CSV/Amazon/Etsy/Google imports.
 */
export {};

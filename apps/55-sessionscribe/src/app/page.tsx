/**
 * src/app/page.tsx
 *
 * Marketing landing page. Built LAST, to MARKETING_PLAYBOOK.md.
 *
 * Message architecture:
 * - Enemy: pajama time — the evening pile of unwritten notes that follows
 *   the clinician home.
 * - One sentence: "The note finished before the next client sits down."
 * - Device: the between-sessions clock. Session ends 3:00 -> draft ready
 *   3:02 -> reviewed, edited, signed 3:07 -> next client 3:10.
 * - Hero: the draft assembling section by section (S, O, A, P stamping in)
 *   against that clock (the 5-second demo, HTML/CSS, LCP <= 2s).
 *
 * TODO:
 * - [ ] Hero with the clock + assembling draft; de-risk line: "No card
 *       required. BAA included."
 * - [ ] Proof: a real (dogfood/permissioned, clearly framed) draft with its
 *       traceability highlights; the signed-note artifact with hash +
 *       timestamp; the retention/BAA trust panel.
 * - [ ] Objection killer: "An AI wrote my chart?" -> No. It drafted; you
 *       signed. Show the sign gate and the audit log.
 * - [ ] The math: your hourly rate x 6-8 hours of weekly notes vs $69/mo.
 * - [ ] Pricing table anchored against one recovered evening per week.
 * - [ ] One CTA phrase repeated verbatim (hero, post-proof, post-pricing,
 *       sticky mobile bar): "Start free — 14 days".
 */

export default function LandingPage() {
  // TODO: implement per MARKETING_PLAYBOOK.md
  return null;
}

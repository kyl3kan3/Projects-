/**
 * Crawler worker (Playwright): index the customer's docs/site.
 * TODO: sitemap-first crawl w/ CRAWL_PAGE_LIMIT; readability extraction ->
 * markdown (turndown); chunking w/ heading context; embeddings; incremental
 * re-crawl (content hash diff); robots.txt respect.
 */
export {};

/**
 * Drizzle schema (Postgres + pgvector): orgs, bots, sources (crawl roots/files),
 * pages, chunks (embedding vector(1536)), conversations, messages,
 * handoffs, unanswered_questions, subscriptions.
 * TODO: define tables; HNSW index on chunks.embedding; message feedback flags.
 */
export {};

/**
 * src/app/page.tsx
 *
 * Marketing landing page. Built LAST, to MARKETING_PLAYBOOK.md.
 *
 * Message architecture:
 * - Enemy: managing three units out of a text thread and a shoebox — and
 *   having nothing on paper the day it goes wrong.
 * - One sentence: "TenantFile runs the tenancy and keeps the file, so the
 *   day you need records, they exist."
 * - Device: the shoebox becoming the File — a tenancy timeline assembling
 *   itself (application -> screened -> signed -> paid -> fixed).
 * - Hero: the File timeline stitching itself in 5 seconds.
 *
 * TODO:
 * - [ ] Hero with the stitching-timeline demo (HTML/CSS, LCP <= 2s).
 * - [ ] The math: your rent roll vs. one month of a property manager's 8%.
 * - [ ] Honest receipts (design-partner tenancies, clearly framed).
 * - [ ] Objection killer: "My tenants pay by Zelle" -> the ledger works
 *       anyway; mark-paid keeps the record true.
 * - [ ] Pricing anchored against one deposit dispute.
 * - [ ] One CTA phrase repeated verbatim: "Start your file".
 */

export default function LandingPage() {
  // TODO: implement per MARKETING_PLAYBOOK.md
  return null;
}

/**
 * Tab bar: Record / Log / Progress / You per DESIGN.md (rubber 94% + blur,
 * hairline top, 22px SVG icons, active = text + 2px plate dot).
 * TODO: implement with the shared icon set — no emoji, no icon fonts.
 */
export default function TabsLayout() {
  return null; // TODO: implement
}

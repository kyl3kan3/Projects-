/**
 * Log tab: sessions grouped by date, hairline set rows (lift, JBM meta,
 * mini path glyph), filter chips; rows open the stored review.
 * TODO: implement; expired-clip rows keep metrics and drop the glyph;
 * empty state uses plausible sample content, never lorem.
 */
export default function LogScreen() {
  return null; // TODO: implement
}

/**
 * Paywall sheet: RevenueCat Offering rendered as hairline-divided plan rows
 * (Annual $79.99 hero w/ 7-day trial, Monthly $12.99), restore purchases,
 * transparent terms.
 * TODO: implement; triggered at the three gates (4th weekly set, first
 * non-squat lift, first fault-trend review); join-intent preserved so the
 * gated action resumes after purchase.
 */
export default function PaywallScreen() {
  return null; // TODO: implement
}

/**
 * Root layout: font loading (Archivo, JetBrains Mono), RevenueCat configure,
 * SQLite migrations, keep-awake during recording, dark-only theme tokens.
 * TODO: implement; block render until fonts + migrations resolve (no
 * system-font flash — a silent fallback is a failed build).
 */
export default function RootLayout() {
  return null; // TODO: implement
}

/**
 * Review screen: clip + the bar-path trace signature (Skia), rep-card rail,
 * verdict summary, rest timer in header, Log this set / Share actions.
 * TODO: implement; scrub bead sync with video position; watermark export on
 * free tier; reduced-motion shows the completed trace instantly.
 */
export default function ReviewScreen() {
  return null; // TODO: implement
}

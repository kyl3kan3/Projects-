/**
 * Skia overlay + export: live skeleton during framing, the bar-path trace
 * signature on review (chalk polyline, plate re-stroke on deviations, JBM
 * annotation at worst point), and the shareable video export (watermarked
 * on free) via offscreen surface.
 * TODO: implement trace geometry from the stored polyline, scrub-bead sync,
 * reduced-motion instant render, and export presets sized for
 * TikTok/Reels/Shorts.
 */

export function renderTrace(_polyline: Array<{ x: number; y: number }>): unknown {
  throw new Error("Not implemented");
}

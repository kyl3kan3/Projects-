/**
 * app/scan-entry.tsx — Scan entry sheet
 *
 * The monitoring-day capture surface (DESIGN.md "Scan entry sheet"):
 * numeric-first, built for a parking-lot entry in under a minute.
 *
 * TODO:
 * - [ ] Sheet at radius 20: date (defaults today), JBM 36 value fields for
 *       E2/LH/P4 with unit labels, lining field (mm).
 * - [ ] Per-ovary follicle chips: fast numeric add (sizes in mm), left/right
 *       columns, running counts.
 * - [ ] Note field for clinic instructions heard at the appointment.
 * - [ ] Save -> scan row via repositories; charts update immediately; offer
 *       the dose-change editor next ("monitoring is when doses change").
 * - [ ] Values stored exactly as entered with units — no ranges, no flags,
 *       no interpretation (wellness line).
 * - [ ] Numeric keyboards, 44px targets, <60s completion target (ROADMAP).
 */

export {};

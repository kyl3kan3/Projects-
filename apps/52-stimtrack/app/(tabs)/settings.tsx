/**
 * app/(tabs)/settings.tsx — Settings
 *
 * Reminder defaults, storage tracker entry, data control, reliability
 * explainer, reference library, and the always-reachable "End this cycle."
 *
 * TODO:
 * - [ ] Reminder defaults + quiet hours, with the explicit line: "Quiet hours
 *       never apply to your trigger reminder."
 * - [ ] Storage tracker row -> /storage.
 * - [ ] Export/backup (JSON via src/lib/backup), delete-all-data (double
 *       confirm), restore purchases.
 * - [ ] Reliability explainer: "Try it: airplane mode. Everything works."
 *       — no account, no server, data stays on device.
 * - [ ] Reference library: the 25 cited explainer cards (educational only).
 * - [ ] "End this cycle" -> loss-aware end flow (src/lib/lossState): typed
 *       reason, one press-and-hold, transactional silence. Free forever,
 *       never buried.
 * - [ ] About/licenses; .env-driven RevenueCat config surfaced nowhere.
 */

export {};

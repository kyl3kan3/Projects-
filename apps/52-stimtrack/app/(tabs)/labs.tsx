/**
 * app/(tabs)/labs.tsx — Scans & labs
 *
 * Monitoring results (DESIGN.md "Labs"): scan list with JBM value columns,
 * E2 and lead-follicle charts with dose-change markers, per-ovary detail,
 * and the cycle-summary PDF action.
 *
 * TODO:
 * - [ ] Scan list newest-first: date, JBM E2/LH/P4 columns (units), lining,
 *       follicle count summary; hairline rows, aligned tabular figures.
 * - [ ] Row tap -> per-ovary follicle detail (sizes as JBM chips).
 * - [ ] "Log scan" -> scan-entry sheet (also reachable from Today).
 * - [ ] E2 chart + lead-follicle chart via src/components/charts: 2px
 *       viridian line, hairline grid, scan-day ticks, dose-change markers.
 * - [ ] "Cycle summary PDF" action (Plus gate) -> /summary.
 * - [ ] Chart/log display only — no target ranges, no interpretation, no
 *       "your E2 should be…" (wellness line).
 * - [ ] Empty state: "No scans yet. After your first monitoring appointment,
 *       log E2 and follicle counts here…" (DESIGN.md voice).
 */

export {};

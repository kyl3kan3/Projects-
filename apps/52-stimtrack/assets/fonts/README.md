Self-hosted variable fonts required by DESIGN.md (download from Google Fonts, all OFL-licensed; commit the files here — do not rely on network loading):
- Archivo-Variable.ttf (Google Fonts "Archivo", OFL) — display voice (600/700)
- Inter-Variable.ttf (Google Fonts "Inter", OFL) — UI / body (400/500/600)
- JetBrainsMono-Variable.ttf (Google Fonts "JetBrains Mono", OFL) — every clinical number: doses, E2 values, follicle sizes, dates, countdown digits (500/600, tabular figures)

A silent system-font fallback is a failed build.

/**
 * src/data/demo.ts
 *
 * The seeded demo cycle (dev/screenshot builds only — never production):
 * a believable IVF cycle at stim day 9 so every screen renders real
 * content (craft rule: no lorem, no placeholder bars).
 *
 * TODO:
 * - [ ] One ivf_fresh cycle, CD 11 / stim day 9: baseline + 3 monitoring
 *       events logged, trigger tentatively 2 days out, retrieval window set.
 * - [ ] Prescriptions: Gonal-F + Menopur (with one dose-change event after
 *       monitoring #2), Cetrotide added day 5 — dose_log history consistent
 *       with the calendar.
 * - [ ] Scans: 3 entries with plausible rising E2, per-ovary follicle
 *       arrays, lining progression (values sanity-checked against published
 *       patient-education ranges; still display-only).
 * - [ ] One storage_item example for the egg-freezing screenshot state.
 * - [ ] Prep notes ("Ask about the Menopur dose").
 * - [ ] Guarded export: only seeded behind the dev flag in db.ts.
 */

export {};

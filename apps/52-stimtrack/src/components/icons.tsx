/**
 * src/components/icons.tsx
 *
 * The single SVG icon set (DESIGN.md "Iconography"): 20x20 viewBox, 1.75
 * stroke, round caps/joins, currentColor. No emoji anywhere, ever.
 *
 * Glyphs to draw: syringe, pill, vial, probe, calendar, clock, snowflake,
 * bank, circle-dots (follicles), chart, file-text, bell, bell-off, check,
 * x, plus, pencil, chevron-left/right/down, settings, lock, share, archive.
 *
 * TODO:
 * - [ ] Icon component over react-native-svg: name prop, size (default 20;
 *       tab 22, inline 18), color via currentColor from context.
 * - [ ] Draw each glyph on the 20x20 grid at stroke 1.75, round caps/joins;
 *       optical alignment pass against the type specimen.
 * - [ ] Type the glyph-name union so unknown names fail typecheck.
 */

export {};

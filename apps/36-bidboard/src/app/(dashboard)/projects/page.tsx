/**
 * src/app/(dashboard)/projects/page.tsx
 *
 * GC home: the bidding board. Mobile-first at 390px per DESIGN.md
 * ("Projects (home)").
 *
 * TODO:
 * - [ ] Server component: active projects with per-project bid coverage
 *       (invited vs submitted counts across packages).
 * - [ ] Project rows: Title name, mono due countdown ("DUE MAR 21 ·
 *       6 DAYS"), coverage line + 4px steel-filled hairline track.
 * - [ ] Status grouping: Bidding / Leveling / Awarded / Archived.
 * - [ ] Thumb-zone primary: **New project**; plan-limit gate with upgrade
 *       prompt when at cap.
 * - [ ] Empty state: the three-step first-run cards (import subs, create
 *       project, send invites).
 */

export default function ProjectsPage() {
  throw new Error("Not implemented");
}

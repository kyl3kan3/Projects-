/**
 * Audio plumbing: mic capture -> PCM chunks upstream; TTS chunk playback
 * queue with interruption support (stop playback when user starts talking).
 * TODO: implement with expo-av; measure and log first-audio latency.
 */
export {};

/**
 * Drizzle schema (see ARCHITECTURE.md): users, profiles, resumes, postings,
 * applications, cover_letters, exports, atsx_reports.
 * TODO: define tables; resume versioning via parent_resume_id; profile as
 * the ground-truth source every AI output must cite.
 */
export {};

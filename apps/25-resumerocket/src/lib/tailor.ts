/**
 * Tailoring engine: posting parse -> gap analysis -> grounded bullet
 * rewrites + quantification prompts. FABRICATION GUARD: every output claim
 * must map to a profile item or render as a flagged suggestion requiring
 * explicit user acceptance.
 * TODO: implement with structured output + guard validation pass.
 */
export {};

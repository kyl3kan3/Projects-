/**
 * Export pipeline: template -> HTML -> Puppeteer PDF; DOCX via docx lib.
 * Single-column ATS-safe templates; watermark on free tier.
 * TODO: implement with typography QA fixtures.
 */
export {};

/**
 * ATS X-ray: render -> re-parse the resume like an ATS (text layer,
 * headings, dates, contact fields), emit parsed-back view + format
 * warnings + keyword coverage. Also powers the free standalone checker.
 * TODO: implement.
 */
export {};

/**
 * Versioned team rulebook: parse + validate .mergemate.yml.
 *
 * TODO:
 * - [ ] schema: enabled categories, severity thresholds, path filters,
 *       custom standards (natural-language rules compiled into the prompt)
 * - [ ] defaults for OSS vs team plans
 */
export {};

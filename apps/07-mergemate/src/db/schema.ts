/**
 * Drizzle schema: installations, repos, review_runs, findings,
 * finding_feedback (dismissed/accepted), subscriptions (per-dev seats).
 * TODO: define tables + indexes; seat counting per installation.
 */
export {};

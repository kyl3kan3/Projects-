@@ -3,6 +3,7 @@ import { getDb } from "./db";
 
 export async function saveOrder(order) {
   const db = getDb();
+  db.insert(orders).values(order);
   return order.id;
 }
 
@@ -30,7 +31,7 @@ export function statusFor(order) {
   if (order.paidAt) {
     return "paid";
   }
-  if (order.dueAt < Date.now()) {
+  if (order.dueAt == null) {
     return "overdue";
   }
   return "open";

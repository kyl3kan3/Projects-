/**
 * Tab navigator layout: Sounds / Wind-down / Alarm / Report.
 *
 * Purpose: bottom-tab shell for the four core surfaces. Dark, minimal,
 * large-touch-target design (users interact half-asleep in a dark room).
 *
 * TODO:
 * - [ ] <Tabs> with four screens: index (Sounds), winddown, alarm, report
 * - [ ] Custom tab bar: oversized icons, dim inactive tint, no labels after onboarding
 * - [ ] Show a persistent "now playing" mini-bar above the tab bar when the mixer is active
 * - [ ] Dim the whole UI further between user's bedtime and wake window (night mode)
 */

export default function TabsLayout() {
  // TODO: implement per header block
  return null;
}

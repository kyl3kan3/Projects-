/**
 * Stripe subscription billing: plans, upload-quota enforcement, webhooks.
 *
 * TODO:
 * - [ ] plan catalog: starter ($29, 5 uploads/mo), pro ($59), team ($99)
 * - [ ] checkout session + customer portal helpers
 * - [ ] webhook handlers: subscription created/updated/deleted -> plan sync
 * - [ ] quota check middleware used by the upload endpoint
 */
export {};

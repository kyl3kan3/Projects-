/**
 * Baseline + anomaly engine: per-service seasonal profiles (DOW x hour),
 * sustained-deviation detection (no single-hour noise), contributor
 * enrichment, deploy-window correlation, dollar-impact ranking.
 * TODO: implement with false-positive budget (<1 alert/account/week).
 */
export {};

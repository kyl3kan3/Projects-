/**
 * Waste analyzer: idle instances (CloudWatch utilization), unattached EBS,
 * aged snapshots, oversized resources -> dollar-ranked findings; powers
 * the free "bill roast" acquisition flow.
 * TODO: implement describe-based scans with per-region fan-out.
 */
export {};

/**
 * Cross-account access: CloudFormation quick-create link generation
 * (external ID per org), assume-role client factory, connect verification,
 * least-privilege policy documentation.
 * TODO: implement; the readable IAM policy IS a trust feature — keep it in
 * a reviewable template file.
 */
export {};

/**
 * Drizzle schema (see ARCHITECTURE.md): orgs, members, aws_accounts,
 * cost_facts (monthly partitions), tag_sets, baselines, anomalies, deploys,
 * budgets, waste_findings, alert_channels, alert_log.
 * TODO: define tables; partition strategy for cost_facts; anomaly state
 * machine (open/ack/resolved).
 */
export {};

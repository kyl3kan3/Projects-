/**
 * Workers: ingestion schedules, baseline refresh, anomaly evaluation,
 * digest builds, waste scans, CUR parse jobs.
 * TODO: implement queues; per-account cadence tuned to CE data latency.
 */
export {};

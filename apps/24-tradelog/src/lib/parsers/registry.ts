/**
 * Parser registry — the moat. One module per broker export format,
 * each fixture-tested, versioned, and drift-detectable.
 * TODO: canonical Execution type; parsers for thinkorswim, ibkr-flex,
 * tradovate, binance; row-level error reporting; format-drift alarms.
 */
export {};

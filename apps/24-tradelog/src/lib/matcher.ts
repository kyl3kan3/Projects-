/**
 * Trade matcher: executions -> round-trip trades. FIFO position tracking,
 * scaling in/out, partial closes, options multi-leg grouping, R-multiple
 * calc when stop is recorded.
 * TODO: implement with exhaustive unit tests — this is where competitors
 * lose users; correctness > features.
 */
export {};

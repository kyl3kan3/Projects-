/**
 * Leak detector: statistical findings ranked by dollar impact with
 * minimum-sample gates (time leaks, revenge trading, winner-cut asymmetry,
 * size drift, setup decay). Descriptive only — never trade advice.
 * TODO: implement checks + plain-language statement templates.
 */
export {};

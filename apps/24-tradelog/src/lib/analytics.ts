/**
 * Analytics engine: win rate, profit factor, expectancy, drawdown, segment
 * breakdowns (setup / time-of-day / DOW / hold time) via SQL window fns.
 * TODO: implement as parameterized queries over closed trades.
 */
export {};

/**
 * Email-reply threading: unique reply-to per thread, inbound webhook
 * parsing, attachment handling into portal files.
 * TODO: implement with Resend inbound (or Postmark) webhooks.
 */
export {};

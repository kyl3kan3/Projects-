/**
 * Drizzle schema (see ARCHITECTURE.md): workspaces, members, clients,
 * portals, timeline_phases, files, approvals, threads, messages, invoices,
 * portal_views, magic_tokens.
 * TODO: define tables; magic_tokens scoped per portal + revocable;
 * approval audit jsonb append-only.
 */
export {};

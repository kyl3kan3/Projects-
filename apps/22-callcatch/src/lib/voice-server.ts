/**
 * Media Streams voice loop: WebSocket audio <-> Deepgram STT -> conversation
 * engine -> streaming TTS, with the safety state machine
 * (faq / qualify / book / message / transfer) and confidence-gated fallback.
 * TODO: implement; NEVER improvise pricing — profile facts only.
 */
export {};

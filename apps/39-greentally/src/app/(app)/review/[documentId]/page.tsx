/**
 * src/app/(app)/review/[documentId]/page.tsx
 *
 * Human review screen for low-confidence extractions: bill image beside
 * editable extracted fields; accepting writes reviewed_by and recomputes.
 *
 * TODO:
 * - [ ] Zoomable bill image (pinch on mobile) next to field list.
 * - [ ] Per-field confidence display; amber underline + first-focus on
 *       low-confidence fields.
 * - [ ] Validator warnings inline (unit sanity, period overlap, duplicate).
 * - [ ] Accept (primary, thumb zone) -> activity_lines write + recompute.
 * - [ ] Reject as hold-to-confirm quiet red action.
 */

export default function ReviewPage() {
  return null; // TODO: implement
}

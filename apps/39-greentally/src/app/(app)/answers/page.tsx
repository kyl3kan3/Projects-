/**
 * src/app/(app)/answers/page.tsx
 *
 * Questionnaire answer bank: framework chips (CDP-style / EcoVadis-style /
 * Custom), mapped answers with interpolated figures and source refs.
 *
 * TODO:
 * - [ ] Framework chip row + READY x OF y mono progress caption.
 * - [ ] Answer rows: framework tag label, question, editable answer text,
 *       mono source refs; Copy as a quiet action.
 * - [ ] Users edit tone, never numbers — figures render from results, not
 *       free text.
 * - [ ] Figures open the provenance thread (shared component).
 */

export default function AnswersPage() {
  return null; // TODO: implement
}

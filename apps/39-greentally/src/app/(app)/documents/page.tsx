/**
 * src/app/(app)/documents/page.tsx
 *
 * Document library: batch upload, extraction status, and the needs-review
 * queue (review rows float to the top).
 *
 * TODO:
 * - [ ] Drag-and-drop + file-picker upload to presigned R2 URLs.
 * - [ ] Hairline document rows grouped by month; status glyphs per DESIGN.md
 *       (accepted check, needs-review magnifier, rejected red text).
 * - [ ] Extracting rows: 2px indeterminate hairline pulse (reduced-motion:
 *       static EXTRACTING label).
 * - [ ] Route into the review screen for low-confidence documents.
 */

export default function DocumentsPage() {
  return null; // TODO: implement
}

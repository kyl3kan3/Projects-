/**
 * src/app/(app)/footprint/page.tsx
 *
 * The money screen: headline tCO2e, coverage meter, scope rows, and the
 * provenance-thread signature interaction (DESIGN.md).
 *
 * TODO:
 * - [ ] Headline block: reporting-year label, mono total, scope split.
 * - [ ] Coverage meter (12 month cells; complete/partial/empty states).
 * - [ ] Scope rows (S1 fuel, S2 electricity w/ location|market toggle,
 *       S3 spend screen) — figures open the provenance thread.
 * - [ ] Thumb-zone primary: "Upload bills" until coverage complete, then
 *       "Generate report".
 * - [ ] Empty state with plausible sample data invitation (no lorem).
 */

export default function FootprintPage() {
  return null; // TODO: implement
}

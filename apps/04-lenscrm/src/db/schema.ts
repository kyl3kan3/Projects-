/**
 * Drizzle schema for LensCRM.
 *
 * Entities (see ARCHITECTURE.md data model):
 *   users, studios, leads, clients, sessions (shoots), booking_pages,
 *   contracts, contract_signatures, invoices, payments,
 *   galleries, gallery_photos, gallery_selections,
 *   workflows, workflow_steps, email_automations
 *
 * TODO:
 * - [ ] define all tables with relations + indexes
 * - [ ] enum types: lead_status, session_type, invoice_status, contract_status
 * - [ ] soft-delete pattern on client-facing records
 */
export {};

/**
 * Booking engine: availability rules -> bookable slots -> session creation.
 *
 * TODO:
 * - [ ] availability model (weekly rules + blackout dates + buffer times)
 * - [ ] slot computation in studio timezone
 * - [ ] public booking-page handler: lead capture + slot hold + deposit
 * - [ ] reschedule/cancel flows with notification triggers
 */
export {};

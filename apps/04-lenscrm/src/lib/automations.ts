/**
 * Workflow automation engine: per-shoot-type step sequences.
 *
 * TODO:
 * - [ ] workflow templates (wedding, portrait, newborn) with relative-date steps
 * - [ ] step executors: send email, create task, send invoice, deliver gallery
 * - [ ] scheduler that advances workflows daily
 */
export {};

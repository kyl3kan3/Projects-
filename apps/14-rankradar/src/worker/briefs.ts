/**
 * Brief worker: live SERP fetch -> competitor heading scrape -> grounding
 * pack -> Claude structured brief (outline/entities/questions/links/word
 * count) -> store + decrement credits.
 * TODO: implement with forced-JSON tool schema + validation retry.
 */
export {};

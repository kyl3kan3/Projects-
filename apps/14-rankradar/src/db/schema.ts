/**
 * Drizzle schema (see ARCHITECTURE.md): users, teams, projects, keywords,
 * rank_checks (partitioned monthly), rank_rollups, briefs, reports,
 * alerts, alert_events, subscriptions.
 * TODO: define tables; composite indexes (keyword_id, checked_at);
 * rollup table for chart queries.
 */
export {};

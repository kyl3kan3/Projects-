/**
 * Fastify API (src/server): auth (magic link + .edu verification), course/
 * source/card/exam routes (zod-validated), WebSocket ingest progress,
 * review-event sync endpoint, server-side RevenueCat entitlement checks
 * on ingest-heavy routes.
 * TODO: implement route registration, jose session verification, presigned
 * R2 upload issuing, and the entitlement cache (short TTL).
 */
export {};

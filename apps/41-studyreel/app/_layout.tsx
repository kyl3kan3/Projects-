/**
 * Root layout: font loading (Newsreader, Figtree, IBM Plex Mono),
 * RevenueCat configure with the account-linked app user id, SQLite
 * migrations, auth/session bootstrap.
 * TODO: implement; block render until fonts + migrations resolve (a silent
 * system-font fallback is a failed build).
 */
export default function RootLayout() {
  return null; // TODO: implement
}

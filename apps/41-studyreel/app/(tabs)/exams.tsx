/**
 * Exams tab: past exams, config sheet (topics, length, format chips),
 * timed full-screen run, cited answer key + self-grade rubrics.
 * TODO: implement; thin-topic exclusion notice (amber) before start;
 * every answer-key entry carries the cite-mark and flips to its passage;
 * first exam generation is a paywall gate.
 */
export default function ExamsScreen() {
  return null; // TODO: implement
}

/**
 * Courses tab: course rows -> course detail (sources with processing
 * status, coverage map with THIN flags, proposed-card approval queue).
 * TODO: implement; Add material sheet (record / files / web-uploader link);
 * ingest progress over WebSocket with the 2px hairline pulse; free-tier
 * ingest cap messaging (server-enforced).
 */
export default function CoursesScreen() {
  return null; // TODO: implement
}

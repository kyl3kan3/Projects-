/**
 * web/app/page.tsx
 *
 * Companion web home: laptop upload dropzone, course list, and entry into
 * big-screen review/exams. Mirrors the mobile design tokens exactly
 * (DESIGN.md); the flip becomes a slide-in source panel on wide screens.
 *
 * TODO:
 * - [ ] Session auth against the Fastify API (magic link).
 * - [ ] Dropzone -> presigned R2 upload -> ingest progress via WS.
 * - [ ] Keyboard grading (1-4) with Label hints in review.
 * - [ ] Entitlement-gated by server verification, never client flags.
 */

export default function WebHome() {
  return null; // TODO: implement
}

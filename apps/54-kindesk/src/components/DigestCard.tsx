// The signature object: r16 card, 2px persimmon top rule, Hanken "Mom, this week",
// three hairline-separated sections, JBM figures, "Kept together with KinDesk" footer.
// Renders in light values regardless of theme, plus the 800ms assembly sequence.
export {};

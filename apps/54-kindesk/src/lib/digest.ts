// The weekly "state of Mom" digest builder: aggregates the week (tasks done/coming,
// spend + balances, next appointments, one note) into typeset HTML -> expo-print PDF
// and share image. Always light-mode values; footer "Kept together with KinDesk".
export {};

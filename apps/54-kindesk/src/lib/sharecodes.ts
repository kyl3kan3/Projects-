// Share codes: human-readable family code generation, and deterministic E2E key
// derivation (PBKDF/HKDF via expo-crypto) — the key never leaves devices.
// TODO: code format (readable-over-the-phone), derivation, validation.
export {};

// SQLite open + versioned migrations per ARCHITECTURE.md schema (family, member, task,
// expense, document, contact, entry, reminder, sync_state, settings). TODO: migrations,
// typed rows, newId(), local-day date helpers (local calendar day, never UTC slice).
export {};

// RevenueCat wrapper: `family` entitlement, offline-safe cached entitlement shared to
// members via synced state (invited members inherit, never see the paywall).
// TODO: gates — first invite, 11th document, expense splitting; launch refresh.
export {};

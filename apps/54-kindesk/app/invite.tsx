// INVITE — the share-code sheet (JBM 600 28, letter-spaced, hairline well) and the
// join-a-family flow: enter code -> derive E2E key -> pull family records.
// TODO: src/lib/sharecodes + sync.join(); Family-gated for the inviter.
export {};

// VAULT — search, tag chips (POA/Insurance/Directives/Financial/Other), tile grid,
// add via camera (expo-image-picker) or file (expo-document-picker). 10-doc free gate.
// TODO: encrypted blob handling per sync engine; "the 2 a.m. rule" empty state.
export {};

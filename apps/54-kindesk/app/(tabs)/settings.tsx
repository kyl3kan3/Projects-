// SETTINGS — family members + share code sheet, contacts & decision log entry points,
// reminder defaults, backup export, delete-all-data, plain-language E2E privacy
// explainer ("the relay only ever sees ciphertext"), restore purchases.
// TODO: leave/transfer family; ciphertext explainer copy per DESIGN voice.
export {};

// DIGEST — the signature. "Mom, this week" card assembles over 800ms (persimmon rule
// draws, sections stagger in, figures count up), then the share button arms.
// Always renders in light-mode values; shares as image and PDF via expo-print.
// TODO: builder from src/lib/digest; reduced-motion 250ms fade.
export {};

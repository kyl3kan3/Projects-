/**
 * Nest — household, plan, privacy (DESIGN.md screen 4).
 *
 * Members list + invite (Plus), plan state with manage link, privacy
 * controls: CSV export (Plus), delete-my-data with its plain-language
 * consequence sheet, sign out.
 *
 * TODO: member rows; entitlement-aware gating; the delete cascade
 * confirmation (type the nest name).
 */

import { Text, View } from "react-native";

export default function NestScreen() {
  return (
    <View>
      <Text>Not implemented: household, plan, privacy controls.</Text>
    </View>
  );
}

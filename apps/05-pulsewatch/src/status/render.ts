/**
 * Public status pages: SSR + edge-cached, custom domains on Team plan.
 *
 * TODO:
 * - [ ] 90-day uptime bars per monitor group
 * - [ ] incident history + subscribe-to-updates (email)
 * - [ ] custom-domain resolution middleware
 */
export {};

/**
 * Alert dispatcher: incident events -> channels (email/Slack/Discord/webhook/SMS).
 *
 * TODO:
 * - [ ] per-rule routing with escalation delays
 * - [ ] dedupe + rate-limit (one alert per incident state change)
 * - [ ] recovery notifications
 */
export {};

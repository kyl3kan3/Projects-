// react-native-svg chart marks per DESIGN.md: TrendChart (2px ink line / ember bars, hairline grid,
// weekly ticks), HeatStrip (16px squares, severity opacity ramp), CycleGapChart,
// DoseChangeMarker (1.5px ember rule + 6px diamond + JBM date). No chart junk.
export {};

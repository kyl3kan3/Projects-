// The core control (DESIGN.md): card tile, domain glyph, symptom title, 4-dot severity row
// (ember outline/30/60/100%). Tap tile = cycle severity; tap dot = set directly; selection haptic.
// TODO: logged-today sage glyph state; 56px enlarged night variant.
export {};

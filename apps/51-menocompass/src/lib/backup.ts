// Encrypted backup: full DB -> JSON -> AES (key in expo-secure-store, passphrase-wrapped) -> share.
// TODO: export, import/restore with schema-version check, CSV export of entries/meds/labs.
export {};

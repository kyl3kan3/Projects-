// Deterministic on-device insight engine — pure arithmetic, no network, no advice.
// TODO: for each (symptom x dose-change) with >=21 days both sides, compare window means;
// emit card only past delta threshold + sample floor; render via the observational template
// "X averaged N% lower/higher in the M weeks after your DATE MED change." + correlation footer.
// Copy rules are a hard boundary (see BUILD.md wellness line).
export {};

// Apple Health import (read-only, optional Plus): sleep hours/interruptions + cycle samples
// mirrored into health_sample. TODO: permission flow, incremental sync on open, graceful denial.
export {};

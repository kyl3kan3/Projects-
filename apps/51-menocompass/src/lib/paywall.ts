// RevenueCat wrapper: offering fetch, purchase/restore, entitlement cache in settings so an
// outage never locks out a payer. TODO: gates — 11th symptom, 2nd medication, report, insights,
// health import; free tier limits (10 core symptoms, 30-day history, 1 med).
export {};

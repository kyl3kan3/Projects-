// SQLite open + migrations per ARCHITECTURE.md schema
// (symptom, symptom_entry, cycle_event, medication, regimen, dose_log, lab_result, health_sample, settings).
// TODO: versioned migrations; typed row interfaces; seed curated symptom library on first run.
export {};

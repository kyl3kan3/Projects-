// MEDS — HRT regimen engine + labs.
// TODO:
// - Medication list rows (kind glyph, dose line, next-due / taken state)
// - Add/edit medication: kind (patch/gel/spray/tablet/vaginal/injection/supplement), dose, schedule builder
// - "Change dose or schedule" -> closes current regimen row, opens new one (timeline event)
// - Adherence week-strip per med (sage/claret/hairline dots)
// - Reminder scheduling via notifications lib on every regimen change
// - Labs: table rows + add-result form (panel, value, unit, date); display only, no interpretation
export {};

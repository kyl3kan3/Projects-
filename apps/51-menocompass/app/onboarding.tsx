// ONBOARDING — life-stage quiz -> symptom picker -> HRT status -> first check-in -> paywall.
// TODO: 4 steps max, each one decision; seed active symptom set from picks; land on paywall
// only after the first check-in completes and a report preview is shown (value first).
export {};

/**
 * src/app/page.tsx
 *
 * Marketing landing page. Built to MARKETING_PLAYBOOK.md: the enemy is the
 * quote written at the kitchen table at 10pm; the device is "walk the job,
 * send the bid from the driveway." Hero shows the machine running -- a real
 * capture-to-estimate recording with line items typing themselves in.
 *
 * TODO:
 * - [ ] Hero: claim ("Send the bid from the driveway."), the signature
 *       estimate-drafting animation running on real trade line items,
 *       primary CTA + "no card" de-risk line.
 * - [ ] The device section: kitchen-table-at-10pm vs driveway-at-4:12pm,
 *       stated as arithmetic (2-5 days -> same hour).
 * - [ ] The math section: quote lag cost calculator teaser per trade.
 * - [ ] Receipts: real design-partner proposals + deposit timestamps only
 *       (never fabricated; staged dogfood output clearly framed until then).
 * - [ ] Objection killer: "It only prices from YOUR price book" section.
 * - [ ] Pricing table (Solo/Crew/Fleet) anchored against evening hours and
 *       one lost job per month.
 * - [ ] One CTA phrase repeated verbatim at hero / post-proof / post-pricing
 *       / sticky mobile bar; four-beat motion budget, reduced-motion fades.
 */

export default function LandingPage(): never {
  throw new Error("Not implemented");
}

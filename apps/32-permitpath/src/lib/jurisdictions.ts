/**
 * src/lib/jurisdictions.ts — Jurisdiction resolution (address -> authority)
 *
 * TODO:
 * - [ ] resolve(address): geocode via Mapbox, point-in-polygon against
 *       jurisdiction boundaries, return city/county/state authority chain
 * - [ ] ambiguity handling: return candidates + confidence, never guess
 * - [ ] coverageStatus(jurisdictionId): mapped / partial / unmapped
 */
export {};

/**
 * src/lib/tracker.ts — Application status tracking state machine
 *
 * TODO:
 * - [ ] statuses: use the canonical ApplicationStatus union from
 *       db/schema.ts (not_submitted -> in_review -> issued -> expired,
 *       + stop_work) -- do not invent a parallel vocabulary
 * - [ ] transition guards + timestamps + who
 * - [ ] inspection notes with photo attachments (R2 keys)
 */
export {};

/**
 * src/app/page.tsx — Marketing landing page
 *
 * Landing per MARKETING_PLAYBOOK.md: enemy = the stopped job and the
 * $2,000 fine; device = 'know every permit before you bid'; hero shows a
 * live requirement lookup; one CTA phrase repeated verbatim.
 *
 * TODO:
 * - [ ] Hero with live jurisdiction lookup demo (staged, honestly labeled)
 * - [ ] Receipts: a real requirement record with verified-at stamp
 * - [ ] Pricing anchored against one fine/stop-work order
 * - [ ] StickyCTA on mobile; reduced-motion path
 */

export default function LandingPage() {
  return null; // TODO: implement
}

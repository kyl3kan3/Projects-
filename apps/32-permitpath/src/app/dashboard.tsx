/**
 * src/app/dashboard.tsx (move to src/app/(dashboard)/page.tsx when built)
 *
 * TODO:
 * - [ ] Jobs list (hairline rows): address, jurisdiction, checklist progress
 * - [ ] 'Expiring in 30 days' rail (licenses + permits), accent-stamped
 * - [ ] Coverage-gap banner when a job's jurisdiction is unmapped
 * - [ ] Empty state with plausible sample job
 */
export {};
